# SunshineBSD targets FreeBSD: everything ships with LF line endings,
# regardless of the platform the tree was checked out on.
* text=auto eol=lf

# Binary assets
*.png binary
*.jpg binary
*.ico binary
